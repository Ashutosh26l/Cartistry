services:
  app:
    build:
      context: ../server
      dockerfile: Dockerfile
    container_name: ecommerce-server
    restart: unless-stopped
    env_file:
      - ./.env.prod
    ports:
      - "80:5500"
